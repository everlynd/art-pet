import { Box } from '@mui/system';
import { Filter } from '../../Components/Filter/Filter';

export const CatalogPage = () => {
    return (
        <Box>
            <Filter />
        </Box>
    );
};
